# Root .env file (optional)
# You can set global environment variables here

# Node Environment
NODE_ENV=development

# Note: Individual packages have their own .env files
# See packages/*/. env.example for package-specific configuration
